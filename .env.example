# Copy to .env. The defaults match docker-compose.yml.
# Host port is 55432 (not 5432) to avoid clashing with a local Postgres.
HOST_DB_PORT=55432
DATABASE_URL=postgresql://lc:lc@localhost:55432/lc
N_ROWS=50000
DIM=128
